import axios from 'axios';
import React, { useEffect } from 'react';
import Router from './router';

const App = (props) => {

    return (
        <div>
            <Router />
        </div>
    );
};

export default App;
